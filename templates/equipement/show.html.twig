{% extends 'base.html.twig' %}

{% block title %}Equipement{% endblock %}

{% block body %}
    <h1>Equipement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ equipement.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ equipement.type }}</td>
            </tr>
            <tr>
                <th>Marque</th>
                <td>{{ equipement.marque }}</td>
            </tr>
            <tr>
                <th>Disponnible</th>
                <td>{{ equipement.disponnible ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>Etat</th>
                <td>{{ equipement.etat }}</td>
            </tr>
            <tr>
                <th>Matricule</th>
                <td>{{ equipement.matricule }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_equipement_index') }}">back to list</a>

    <a href="{{ path('app_equipement_edit', {'id': equipement.id}) }}">edit</a>

    {{ include('equipement/_delete_form.html.twig') }}
{% endblock %}
